spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/training_log
    username: postgres
    password: 12345_Cool
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
